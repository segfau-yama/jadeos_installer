This directory is copied into the Live ISO at `/etc/jadeos-installer/templates`.

It is the place to stage the offline JadeOS configuration template that the
GUI installer will eventually write into `/mnt/etc/nixos` before calling
`nixos-install --flake`.
